# Test fixture review — 2026-09-20

本次审查 14 条测试修补，保留 4 处有明确依据的小改动，另外 10 条已经恢复为 GitHub 上游原文。
对照的 `PaddlePaddle/community` master 提交为 `359339f1b2ccb374d683c3b5bcd1cb4f0294834e`。
恢复的 10 个 `test.patch` 已逐个核对字节及 SHA-256，与该上游提交完全一致。

## 逐题结论

| Task | 当前处理 | 原因 |
| --- | --- | --- |
| #50086 | 已恢复上游 | 修改了 Scope 生命周期验收和用例分类；目前只验证了截取代码和替身，尚不足以确认真实 Scope 生命周期下的等价性。 |
| #53534 | 已恢复上游 | 递归加载器仍省略导入和装饰器，NumPy 模拟还会拒绝显式 object 数组路径，不能据此可靠判断不同实现。 |
| #59127 | 保留小修正 | 题目要求混合 dtype 输入报错，未指定动态图异常类别；仅接受 TypeError/ValueError，其他数值、形状及静态检查保持不变。 |
| #70469 | 已恢复上游 | 保留了特定辅助函数名限制；重复警告的验收与 warnings 过滤规则尚未统一。 |
| #74444 | 已恢复上游 | 以简化 Tensor 和受控 dropout 替代真实运算，仍限制函数位置和部分提示文字，尚未确认不同合法实现均可验证。 |
| #74586 | 已恢复上游 | 新增辅助函数抽取会移除装饰器，仍遗漏导入或跨文件依赖；用例还绑定内部调用方式。 |
| #74594 | 保留小修正 | 仅将已有的同一个 core 模拟对象传入被测函数，使直接调用 core.broadcast_shape 与原有包装调用得到一致环境；断言不变。 |
| #78452 | 已恢复上游 | 改为 find_class 探测改变了验收入口和用例分类，对 range 等类型的拒绝要求与题意尚未核清。 |
| #78522 | 已恢复上游 | 已复现等价的 getpid 别名调用无法被定位；测试仍绑定源码形态。 |
| #78570 | 保留小修正 | 仅补充 Paddle 公开的 enable_grad 入口，复用同一梯度状态上下文；保留 loss、梯度开启及更新行为断言。 |
| #79161 | 保留小修正 | 题目明确要求 ValueError，未要求固定英文原文；仅去掉英文匹配，保留异常类型和无副作用检查。 |
| #79167 | 已恢复上游 | 已复现仅改变内部模块导入别名即误判，补入 random 并未完成真实导入处理。 |
| #79275 | 已恢复上游 | Qwen 复测暴露 functools 导入丢失和固定导出列表要求，现有修改仍未保证验收独立于实现。 |
| #79310 | 已恢复上游 | Qwen 源码已有 in_dygraph_mode 导入，测试抽取时仍丢失；补齐少数名字不足以证明通用加载正确。 |

保留的修改只涉及 `test.patch`。题目要求、参考代码和 `test.sh` 均未修改，F2P/P2P 分组未重新划分。
其中 #74594 和 #78570 补的是现有 API 对应的测试依赖；#59127 和 #79161 去掉题意没有规定的异常限制。
这四处改动不依赖 DS 或 Qwen 的私有函数名，不代表整套测试已覆盖所有合法实现。
恢复上游也不代表上游测试本身没有问题；这里只撤回尚不能充分证明正确的修补。

## 验证证据

保留的 4 个补丁与 2026-09-18 直接执行各题 `test.sh` 时的内容完全一致。
本次重新核对了补丁、实际测试文件、`test.sh`、运行计划及观察结果的哈希；下表引用该次执行结果，并非宣称今天重跑了全部用例。

| Task | 原代码：通过 / 失败 | 参考实现 | DeepSeek 已保存答案 | Qwen 已保存答案 |
| --- | --- | --- | --- | --- |
| #59127 | 42 / 84 | 126 通过 | 126 通过 | 环境无效，126 个用例未执行 |
| #74594 | 1 / 2 | 3 通过 | 3 通过 | 3 通过 |
| #78570 | 3 / 6 | 9 通过 | 9 通过 | 9 通过 |
| #79161 | 1 / 2 | 3 通过 | 3 通过 | 3 通过 |

合计 141 个预期用例：原代码 47 通过、94 失败；参考实现及 DeepSeek 答案各 141 通过。
Qwen 在另外 3 题共 15 个用例通过。#59127 的运行环境混用了不同版本的源码和安装包：
覆盖进去的 `tensor/__init__.py` 导入 `histogramdd`，承载运行的安装包却没有这个符号，导致 Paddle 在测试前导入失败。
因此，这一题不能记为 Qwen 通过，也不能记为 Qwen 功能修复失败。

2026-09-20 另外执行了 52 项独立检查，没有采用模型答案作为样例：

| Task | 检查内容 | 合法样例被接受 | 错误样例被拒绝 |
| --- | --- | --- | --- |
| #59127 | 混合 dtype 抛 TypeError/ValueError；不报错或抛其他错误仍失败 | 2 | 2 |
| #74594 | 直接调用 core 或包装函数、不同局部变量名；错误形状仍失败 | 8 | 2 |
| #78570 | 两种开启梯度 API、三种优化器；没开梯度、丢失返回值或遗漏更新仍失败 | 18 | 9 |
| #79161 | 不同英文及中文报错文字；不抛错或异常类型错误仍失败 | 9 | 2 |

52 项检查均符合预期（37 个合法样例被接受，15 个错误样例被拒绝）。
这些检查验证的是四处小修改的边界，不替代完整 Paddle 环境的集成验证。
容器禁用网络、根目录只读、无宿主目录挂载；镜像、输入及结果哈希见同名 JSON。
逐题处理、节点列表和执行证据见 [JSON 记录](test-fixture-review-20260920.json)。
